import React from 'react';
import styled from './index.scss'

class Header extends React.Component {
  render() {
    return (
      <div className={styled.title}>About</div>
    )
  }
}

export default Header